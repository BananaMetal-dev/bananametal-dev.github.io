# Bnana Web Audio Player 仕様書 v0.1

作成日: 2026-07-30
最終更新日: 2026-08-01
作業フォルダ: C:\Users\Banan\Desktop\AI\HP
対象: GitHub Pages 配置用のブラウザ版音楽プレイヤー

## 1. 位置づけ

本仕様書は、Androidネイティブ版 Bnana Media Player とは別系統で開発する、ブラウザ版音楽プレイヤーの仕様を定義する。

Androidスマホ版とWeb版は、現時点では開発を切り離す。Android版の実装、UI、Media3、Room、SAF、バックグラウンド再生仕様は、本Web版の直接前提にしない。

Web版は GitHub Pages 上に配置し、ユーザーがブラウザで開いて使う。音楽ファイルはサーバーへ送信せず、ユーザー端末内のブラウザ処理だけで扱う。

## 2. 開発・運用ルール

- 作業フォルダは `C:\Users\Banan\Desktop\AI\HP` とする。
- 以後、Web版に関する仕様変更が発生した場合は、本仕様書を追従更新する。
- Androidスマホ版とWeb版の仕様は混在させない。
- GitHub Pages公開用の静的Webアプリとして構成する。
- 音源データを外部サーバーへアップロードしない。
- 外部API、クラウド保存、ユーザー認証はMVPでは使用しない。

## 3. 目的

ブラウザ上でローカル音楽フォルダを選択し、テンポを維持したまま、指定した1つのキー変更値で音楽を再生する。キー変更時は現在曲を端末内で変換し、処理完了後に同じ再生位置から再開する。さらに、現在再生中の1曲を指定キーの`.wav`としてダウンロードできるようにする。

将来的には `.mp3` 変換も検討するが、初期実装では `.wav` 出力のみとする。

## 4. 対象環境

### 4.1 初期ターゲット

- Android Chrome
- Windows Chrome
- GitHub Pages HTTPS環境

### 4.2 実機検証が必要な項目

- Android Chromeでのフォルダ選択可否
- サブフォルダ再帰走査の可否
- Web Audio APIの安定性
- キー変更処理のCPU負荷
- `.wav` ダウンロードの挙動
- 画面オフ、バックグラウンド、Bluetooth操作の制限

ブラウザ版では、Androidネイティブアプリと同等の画面オフ再生、バックグラウンド再生、Bluetoothメディアボタン制御は前提にしない。

## 5. 対応ファイル

### 5.1 入力対応形式

- `.mp3`
- `.m4a`
- `.wav`

### 5.2 出力形式

初期実装では `.wav` のみ。

将来フェーズで `.mp3` 変換を検討する。`.mp3` 出力には追加ライブラリ、処理負荷、ライセンス確認が必要となる。

## 6. ファイルアクセス仕様

### 6.1 フォルダ指定

ユーザーはブラウザ上で音楽フォルダを選択する。

MVPの選択方式:

- `<input type="file" webkitdirectory>` を優先する
- `File System Access API` の `showDirectoryPicker` は、ブラウザ間の挙動差が解消するまで使用しない

MVPでは、Android Chromeでの実動作確認を優先する。

### 6.2 サブフォルダ

選択フォルダ配下のサブフォルダも再帰的に走査する。

### 6.3 保持する情報

曲一覧として保持するのは以下に限定する。

- ファイル名
- 相対パス
- 更新日時
- ファイルサイズ
- 拡張子
- File または FileSystemFileHandle

音声データ本体は一覧作成時に全件読み込まない。

### 6.4 重複除外

重複判定は、初期仕様では以下の組み合わせとする。

- ファイル名
- ファイルサイズ

同一判定されたファイルは曲一覧から重複除外する。

## 7. メモリ設計

1万曲規模を想定し、音源データを全曲メモリへ読み込まない。

### 7.1 読み込み対象

同時に読み込む音声データは原則として以下のみ。

- 現在再生中の曲
- 次に再生する曲

フォルダ選択時は曲一覧のメタ情報だけを作成し、音声データのデコードとキー変換は開始しない。選択中の曲に対する音声処理は、ユーザーが再生ボタンを押した時に開始する。これにより、曲一覧の登録完了後は再生、前、次、停止を直ちに操作可能とする。

### 7.2 曲遷移時の処理

曲が次へ移った場合:

1. 旧現在曲の音声バッファを破棄する。
2. 旧次曲の音声バッファを現在曲として扱う。
3. さらに次の曲を先読みする。
4. 曲リスト表示を現在曲基準へ更新する。

前の曲へ戻る場合は、必要に応じて前曲を読み直す。常時保持するのは現在曲と次曲だけを原則とする。

### 7.3 キー変換キャッシュ

同じ曲で同じキーへ戻る際の再変換を避けるため、現在曲の変換済みAudioBufferをキー値別にメモリ保持する。キャッシュは現在曲だけを対象とし、フォルダ変更、曲変更、並び替えで現在曲が変わった時に破棄する。ブラウザを閉じた後まで保持する永続キャッシュは使用しない。

保持モード:

- モバイル保持: 初期設定。キー0と現在選択中または直近の変換キーを合わせて最大2種類保持する。キー0以外、または現在曲で2種類を保持中の場合は、次曲の変換済みバッファを先読みしない。
- PCキャッシュ: 現在曲を最大13種類、推定1.5GiBまでLRU方式で保持する。次曲は現在選択中のキー1種類を先読みする。

キー0と現在選択中のキーは優先保持するため、この2種類だけでモードの推定容量上限を超える場合がある。PCキャッシュでも無制限保持にはせず、曲長、サンプルレート、チャンネル数に応じて古い変換キーを破棄する。

画面には、現在曲で保持中のキー、次曲の先読みキー、AudioBuffer本体から算出した推定使用量を表示する。推定値にはブラウザ内部の管理領域や変換中の一時メモリを含めない。

## 8. 再生仕様

### 8.1 基本操作

- 再生
- 一時停止
- 停止
- 次の曲
- 前の曲
- 音量調整

### 8.2 並び順

- 名前順
- 新着順
- ランダム

新着順はファイル更新日時の降順とする。

### 8.3 ループ

最後の曲が再生し終わった場合は、最初の曲へループする。

### 8.4 再生中リスト表示

再生中は、曲リストの上部を現在曲中心に表示する。

- 1行目: 再生し終わった直前の曲
- 2行目: 現在再生中の曲
- 3行目: 次に再生する曲
- 4行目: その次に再生する曲

先頭曲再生中の場合、1行目には最後の曲を表示する。

## 9. キー変更仕様

### 9.1 基本仕様

指定キーは1つだけ持つ。

- 初期値: `0`
- フォルダ読み込み時: 常に `0` へ戻す
- 単位: 半音
- 推奨範囲: `-6` から `+6`

例:

- `-2`
- `0`
- `+3`

### 9.2 テンポ固定処理

現在曲と次曲に対して、SoundTouchJSのオフライン処理で指定キーを適用したAudioBufferを作成する。変換済みバッファは再生速度を常に1倍として再生し、キー値によってテンポを変化させない。

再生中に未保持のキーへ変更した場合:

1. 連続操作中はキー表示だけ更新し、最後の入力から500ms待つ。
2. 最後に選択されたキーだけを変換対象とする。
3. 現在の再生位置を記録する。
4. 再生を一時的に止め、処理中表示にする。
5. 新しいキー値で現在曲を変換する。
6. 変換完了後、記録した再生位置から自動再開する。
7. 変換結果を現在曲のキーキャッシュへ保持する。
8. PCキャッシュでは次曲を同じキー値でバックグラウンド先読みする。

完全な無待機リアルタイム反映はMVPの対象外とする。カラオケ用途では反映速度よりテンポ維持を優先する。

実際の変換処理中はキー操作を無効化し、変換の競合を防止する。キャッシュ済みキーを選択した場合は再変換を行わず、保持済みAudioBufferへ切り替える。

停止中にキー値を変更した場合は、その場では音声変換を行わない。新しいキー値を保存し、次に再生ボタンを押した時に現在曲へ適用する。再生中または一時停止中のキー変更では、再生位置を保持して直ちに変換を開始する。

### 9.3 技術候補

- Web Audio API
- SoundTouch系ライブラリ
- オフラインAudioBuffer変換
- 将来候補としてWASMまたは高品質リアルタイムDSP

MVPでは、SoundTouchJSのオフライン処理と通常速度のWeb Audio API再生を組み合わせる。

## 10. `.wav` ダウンロード仕様

### 10.1 対象

ダウンロード変換の対象は、現在再生中の1曲のみとする。

### 10.2 処理内容

現在設定されているキー値で音声を処理し、`.wav` ファイルとしてブラウザからダウンロードする。

### 10.3 ファイル名

出力ファイル名の形式:

```text
元ファイル名_key+2.wav
元ファイル名_key-3.wav
元ファイル名_key0.wav
```

### 10.4 保存方式

初期実装ではブラウザのダウンロード機能を使用する。

将来的に File System Access API による保存先指定を検討する。

## 11. UI仕様案

アプリ名は `Banana Key Changer` とする。

UIはAndroid版で採用した方向性を引き継ぎ、暗い木材、真鍮、銅管を基調とした抑制的なスチームパンク調とする。スマホ縦持ちでは曲リストを上部、キー変更と再生操作を下部へ配置し、片手操作しやすい構成とする。Windows Chromeでは曲リストを左、現在曲と操作部を右に配置する2カラム構成とする。

### 11.1 主要領域

- 曲リスト
- 現在曲表示
- 次曲表示
- 再生コントロール
- 並び順切替
- キー変更コントロール
- `.wav` ダウンロードボタン
- フォルダ選択ボタン
- フォルダ読み込み・曲順状態表示
- 再生・キー変換・WAV保存状態表示

フォルダ読み込みと曲順の状態は曲リスト上部に表示する。再生、停止、キー変換、キャッシュ切替、WAV保存の状態は、操作中に視線を移動させず確認できるようキー変更ボタンの直上へ表示する。

上部タイトル領域には、機能要約として `テンポを保ってキー変更・WAV保存` を表示する。併せて、音楽ファイルは外部へ送信されず、音声処理がブラウザ内で完結することを明記する。この案内は音源データを対象とし、ページ表示に必要な静的ファイル配信まで通信がないと誤認させない表現にする。

### 11.2 キー変更UI

- `キー -`
- `キー 0`
- `キー +`
- 現在値表示: `Key +2`

キー変更処理中は、対象曲名とキー値を状態欄へ表示し、キー変更ボタンを一時的に無効化する。

キー目盛りでは、選択中キーと変換データ保持中キーを別の色で表示する。キー操作部には以下を常時表示する。

- 現在曲で保持中のキー一覧
- 次曲で先読み中のキー
- 推定メモリ使用量
- 左をスマホ、右をPCキャッシュとして中央の丸が選択側へ移動する2択トグル

トグルでは選択中の側を明るい文字色でも示す。キー操作部には、スマホが現在曲を最大2キー、PCキャッシュが最大13キーまで一時保持すること、曲変更時に消去すること、PCキャッシュはメモリ使用量が増えることを短文で明記する。

### 11.3 ダウンロードUI

ボタン名案:

- `現在曲をWAV保存`

処理中は以下を表示する。

- 変換中
- 進捗率
- キャンセル可否
- 完了後のダウンロードリンクまたは自動ダウンロード

MVPで採用するSoundTouchJSのオフライン処理APIは途中進捗率とキャンセルを公開していないため、初期実装では処理中表示と自動ダウンロードだけを行う。進捗率表示とキャンセルは後段検討とする。

## 12. データ保存仕様

ブラウザ内に保存してよい情報:

- 並び順設定
- 音量
- キャッシュモード
- 最後の再生位置
- 曲一覧のメタ情報

音源本体とキー変換データをIndexedDBへ大量保存する設計はMVPでは採用しない。キー変換キャッシュは現在のブラウザタブのメモリ内だけで保持する。

## 13. セキュリティ・プライバシー

- 音源ファイルをサーバーへ送信しない。
- GitHub Pagesは静的ファイル配信のみとする。
- 外部APIへ音源データを渡さない。
- 上部タイトル領域に、音楽ファイルを外部送信せずブラウザ内で処理する旨を常時表示する。
- 変換処理はブラウザ内で完結する。
- 利用者が権利を持つ音源、または利用許諾のある音源を対象とする旨をUIまたはREADMEへ記載する。

## 14. 主要リスク

### 14.1 Android ChromeのAPI制約

フォルダ選択、サブフォルダ走査、ファイルハンドル保持はブラウザ実装差がある。最初に実機確認が必要。

### 14.2 音声処理の負荷

キー変更用のAudioBuffer変換はCPU負荷と一時的なメモリ使用量が大きくなる可能性がある。スマホでの処理時間、発熱、メモリ不足を検証する。

4分36秒、48kHz、ステレオの実音源では、Float32 AudioBuffer 1種類の本体サイズは約101MiBとなる。PCでも利用可能メモリとブラウザ実装によってタブ停止やクラッシュの可能性があるため、PCキャッシュを無制限保持にはしない。推定使用量表示は目安であり、実際のプロセスメモリ使用量を保証する値ではない。

キー変更処理には `@soundtouchjs/audio-worklet` 2.1.0のオフライン処理APIを使用する。ライセンスはMPL-2.0であり、公開物にライセンス表示を同梱する。

### 14.3 バックグラウンド再生

ブラウザ版では画面オフやバックグラウンドでの継続再生が制限される可能性がある。Androidネイティブ版と同等の安定性は前提にしない。

### 14.4 `.mp3` 出力

`.mp3` 出力は後段対応とする。実装前にライブラリ、ライセンス、処理速度、ファイルサイズを確認する。

## 15. MVPロードマップ

### Phase 1: 土台

- GitHub Pages用の静的Webアプリ雛形を作成
- フォルダ選択
- `.mp3` `.m4a` `.wav` の一覧化
- サブフォルダ再帰走査
- 重複除外

### Phase 2: 軽量再生

- 現在曲と次曲だけ読み込み
- 再生、一時停止、停止、次、前
- 名前順、新着順、ランダム
- 最後から先頭へのループ
- 現在曲中心の曲リスト表示

### Phase 3: キー変更

- キー値1つを保持
- 半音単位のキー変更UI
- 現在曲と次曲へのテンポ固定キー変更適用
- 再生中のキー変更後に同じ位置から再開
- スマホChromeで処理時間、音質、負荷を確認

### Phase 4: `.wav` 保存

- 現在曲を指定キーで処理
- `.wav` Blob生成
- ダウンロード機能
- 変換中表示
- エラー表示

### Phase 5: 公開整備

- GitHub Pages公開
- README整備
- 利用上の注意
- 既知の制限
- 実機検証結果の記録

### Phase 6: 後段検討

- `.mp3` 出力
- 保存先指定
- 実機試験後のUI調整
- PWA化
- ブラウザキャッシュ最適化

## 16. 現時点で未決定の事項

- Android Chromeでのフォルダ選択API
- PWA対応の有無

## 17. 実装決定事項

- Webアプリ名: `Banana Key Changer`
- キー変更範囲: `-6`から`+6`の半音単位
- キー変更処理: SoundTouchJS 2.1.0のオフライン処理で現在曲と次曲を変換
- 再生速度: キー値にかかわらず常に1倍
- 再生中のキー変更: 処理完了後に変更前と同じ再生位置から自動再開
- 連続キー操作: 最後の入力から500ms後に最終キーだけを処理
- キャッシュ済みキー: 再変換せず保持済みAudioBufferを再利用
- キャッシュ初期値: モバイル保持
- モバイル保持: 現在曲最大2キー。キー0以外、または2キー保持中は次曲変換を先読みしない
- PCキャッシュ: 現在曲最大13キー、推定1.5GiB上限、次曲1キーを先読み
- キャッシュ破棄: フォルダ変更、曲変更、現在曲が変わる並び替え
- キャッシュ表示: 保持キー、次曲キー、推定メモリ使用量
- 読み込み開始時点: フォルダ選択時はメタ情報のみ。音声デコードとキー変換は再生操作時に開始
- 停止中のキー変更: 値だけを保存し、次回再生時に処理
- WAV処理: 再生用に変換済みの現在曲バッファを16bit PCMへエンコード
- WAVサンプルレート: 入力音源のサンプルレートを維持
- WAVビット深度: 16bit PCM
- WAVチャンネル数: 入力の1chまたは2chを維持し、3ch以上は2chまでを出力
- WAV上限: RIFF/WAVの制約により4GB未満
- 変換進捗: MVPでは処理中表示のみ
- 変換キャンセル: MVPでは非対応
- フォルダ選択: 内蔵ブラウザとの互換性を優先し、`webkitdirectory`付きファイル入力を使用
- 曲一覧: 1万曲を考慮してデスクトップ表示を仮想スクロール化
- ランダム: `crypto.getRandomValues`を使ったFisher-Yatesシャッフル
- UI: スマホ縦持ちを優先したスチームパンク調、デスクトップは2カラム

## 18. 実装・検証状況

2026-08-01時点で、フォルダ走査、重複除外、3種類の並び順、現在曲と次曲のデコード保持、再生、一時停止、停止、前、次、音量、キー変更、キー別キャッシュ、WAV保存の初期実装が完了している。

Windows上のPlaywright Chromiumで実音源試験を実施した。試験対象は `D:\BananaMetalBand\BM3\Cover` 以下のMP3 1曲、WAV 17曲、合計18曲である。

確認済み項目:

- 直下とサブフォルダを含む18曲の再帰取得と一覧表示
- WAVとMP3のデコード、再生進行、一時停止位置保持、再開、次曲への移動と自動再生
- 名前順、新着順、ランダム順への切替
- ランダムを連続実行した場合に先頭曲が連続して同じにならないこと
- 再生中のキー値変更表示と、キー `+1` および `+2` を指定したWAVダウンロード
- 出力WAVが48kHz、ステレオ、16bit PCM、276.651667秒で、元MP3と同じ再生時間であること
- 出力WAVの全編デコード成功と、無音ではないこと
- キー+1の変換済み音源が、実時間5.002秒に対して5.000秒進み、キー変更後も1倍速で再生されること
- 再生中にキー+1から+2へ変更した際、50.141秒から50.203秒へ復帰し、再生位置のずれが約0.061秒であること
- キー変更後の再開から3秒間で再生位置が3.019秒進むこと
- 同じキーで先読みした次曲へ約0.06秒で遷移して自動再生されること
- キー+2の出力WAVが48kHz、ステレオ、16bit PCM、276.651667秒で、元MP3と同じ再生時間であること
- 保存済みキー+2の状態でも、18曲の一覧登録直後に前、再生、停止、次ボタンが有効になること
- 曲を選択し直した後も音声処理を開始せず、再生ボタンが有効のままになること
- 再生ボタンを押した時だけキー変換を開始し、変換完了後に自動再生へ移ること
- 停止中のキー変更では変換を開始せず、再生と次ボタンが有効のままになること
- フォルダ読み込み時にキー0、保持なしへ戻り、キャッシュモード設定だけが維持されること
- モバイル保持でキー0の現在曲と次曲を保持し、推定203MBと表示されること
- キー+を3回連続操作した際、+1と+2を変換せず、500ms後に+3だけを変換すること
- モバイル保持でキー0と+3だけを推定203MBで保持すること
- 保持済みキー0へ117ms、保持済みキー+3へ116msで切り替わり、再変換しないこと
- PCキャッシュで現在曲のキー0、+3、+4と次曲+4を保持し、推定405MBと表示されること
- PCキャッシュで保持済みキー+3へ68msで切り替わり、キー+4が破棄されないこと
- PCキャッシュをオフにするとモバイル上限へ縮小し、5秒後も旧モードの次曲先読みが復活しないこと
- PCキャッシュで現在曲0、+1と次曲0を304MB保持中にモバイルへ戻すと、次曲0を破棄して203MBとなり、5秒後も復活しないこと
- 同一フォルダを同じ先頭曲の状態で再読込しても、キー0、保持なしへ初期化されること
- 390×844のスマホ幅で保持状況、推定使用量、PCキャッシュトグルが重ならず表示されること
- フォルダと曲順の状態は曲リスト上部、再生・変換・保存の状態はキー変更ボタン付近に分離して表示されること
- スマホとPCキャッシュの選択側がトグルの丸位置と文字色で判別でき、保持上限と消去条件の説明文が表示されること
- ページ見出し、ブラウザタイトル、アプリ一覧で `Banana Key Changer` と表示されること
- 上部タイトル領域に機能要約と、音楽ファイルを外部送信せずブラウザ内で処理する案内が表示されること
- 1365×768と390×844でタイトル、案内文、Appsボタンが重ならず、横スクロールが発生しないこと
- 試験中のブラウザコンソールにエラーと警告がないこと
- 音源を外部送信する非静的ネットワーク要求がないこと
- 内蔵ブラウザで `showDirectoryPicker` が選択結果を `AbortError` として返す事象を確認し、`webkitdirectory` 方式へ変更

修正履歴:

- SoundTouchJS AudioWorkletによるリアルタイム処理では、手動試験でキーを上げるとテンポが速く、下げると遅くなる事象を確認した。
- カラオケ用途の要件を満たさないため、再生速度1倍の変換済みAudioBufferを再生する方式へ変更した。

未検証項目:

- Windows Chrome実機での出音とキー変更後の聴感品質
- 修正後のテンポ固定方式について、Windows ChromeとAndroid Chromeでの聴感確認
- Android Chromeでのフォルダ選択、再生、WAV保存
- 1万曲フォルダでの走査時間と画面応答
- 長時間音源のWAV変換時メモリ使用量
- 最終曲の再生完了後に先頭曲へ戻る長時間試験
- Bluetooth、画面オフ、バックグラウンド時のブラウザ制約
- npmが報告した重大度Highの依存関係脆弱性1件の原因特定

## 19. 受け入れ基準 MVP

- GitHub Pages上でページが開ける。
- ユーザーが音楽フォルダを選択できる。
- 対象音楽ファイルの一覧が表示される。
- 音源データの読み込みが現在曲と次曲中心で行われる。
- 再生、停止、次、前が動作する。
- 最後の曲の次に最初の曲へ戻る。
- キー値を1つ指定できる。
- キー変更後、テンポを維持した変換音源が同じ再生位置から自動再開される。
- 再生・変換状態がキー変更操作の近くで確認できる。
- スマホ保持とPCキャッシュの選択状態および違いが画面上で判別できる。
- 現在曲を指定キーで`.wav`としてダウンロードできる。
- 音源がサーバーへ送信されない。
